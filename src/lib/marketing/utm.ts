export const campaignUrl = (destination: string, platform: string, productSlug: string, assetId: string) => { const url = new URL(destination); url.searchParams.set('utm_source', platform.toLowerCase().replace(/[^a-z0-9]+/g, '_')); url.searchParams.set('utm_medium', 'organic_social'); url.searchParams.set('utm_campaign', productSlug.toLowerCase()); url.searchParams.set('utm_content', assetId.toLowerCase()); return url.toString(); };
