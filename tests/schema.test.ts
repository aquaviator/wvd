import {describe,expect,it} from 'vitest'; import {productSchema,funnelSchema} from '../src/schemas/manifests'; import {campaignUrl} from '../src/lib/marketing/utm';
describe('manifest schemas',()=>{it('rejects invalid identifiers and prices',()=>{expect(()=>productSchema.parse({product_id:'bad'})).toThrow();});it('accepts a no-offer funnel',()=>{expect(funnelSchema.parse({checkout:{provider:'etsy',provider_url_or_id:null},offers:{order_bump:null,bundle_upgrade:null,cross_sell:null,upsell:null},funnel_version:'1'}).offers.upsell).toBeNull();});it('generates deterministic campaign URLs',()=>{expect(campaignUrl('https://example.com/p','LinkedIn','My-Product','WVD-X-001-C001')).toContain('utm_medium=organic_social');});});
